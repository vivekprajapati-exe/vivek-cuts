
import { useEffect, useState } from "react";

// Simplified version that doesn't use Lenis
export const useSmoothScroll = () => {
  // Return null to indicate no smooth scrolling is active
  return null;
};
